# DocsBuddy — Vite Starter

Minimal Vite setup created by assistant.

Quick start (Windows `cmd.exe`):

```cmd
cd "c:\\Users\\Lovely Kumari\\Desktop\\DocsBuddy"
npm install
npm run dev
```

Available scripts:

- `npm run dev` — start dev server
- `npm run build` — build production
- `npm run preview` — preview production build
